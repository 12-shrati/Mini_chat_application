import 'package:flutter/material.dart';

/// Application theme and styling configuration
class AppTheme {
  // Primary Colors
  static const Color primaryColor = Colors.deepPurple;
  static const Color accentColor = Colors.teal;
  static const Color backgroundColor = Colors.white;

  // Message Colors
  static const Color senderMessageColor = Colors.deepPurple;
  static const Color receiverMessageColor = Color(0xFFF0F0F0);
  static const Color senderTextColor = Colors.white;
  static const Color receiverTextColor = Colors.black;
}
